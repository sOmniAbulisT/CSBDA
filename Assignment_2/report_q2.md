# Assignment 2 Question 2

## Step 2a

**1. Approach and Decision:**
I chose Python to complete this task. First, I removed the unnecessary R script and renamed Python script to `a2_q2.py`. After that I defined a Python 
function named `a2_q2`, which takes a dictionary as input and returns a dictionary containing the results of four oprations: `sum`, `difference`, 
`product`, and `ratio`, to meet the testing requirements of `a2_q2_v0`.

**2. Git Commands Used:**
* `git rm intern.R`
* `git mv intern.py a2_q2.py`
* `git add a2_q2.py`
* `git commit -m "Q2a: ..."`
* `git push origin main`
* `git log --oneline`

**3. Relevant Commit IDs:**
* `[6fc51b0]` Q2a: reomve R script and rename Python script to a2_q2.py
* `[866e20a]` Q2a: rename function to a2_q2 and rename parameter n to input
* `[31e7231]` Q2a: implement sum, different, product, and ratio calculation
* `[4e50aa8]` Q2a: add missing comma in dictionary syntax

## Step 2b

**1. Approach and Decision:**
From the error messages generated by the `a2_q2_v1` test on the website server. I deduced that customer's requirements have changed to exponentiation
operations. The function input the dictionary with two positive integers and must return the dictionary with two new keys: 
`power_xy` (the first number to the power of the second number) and `power_yx` (the second number to the power of the first number).
Finally, I removed the older arithmetric operations (sum, difference, product, and ratio) and implement the new operation. 

**2. Git Commands Used:**
* `nano a2_q2.py`
* `git add a2_q2.py`
* `git commit -m "Q2b: ..." -m "..."`
* `git push origin main`

**3. Relevant Commit IDs:**
* `[e6bf029]` Q2b: (debug) correct dictionary syntax from == to :
* `[355ba7b]` Q2b: implement y to the x
* `[063faa6]` Q2b: implement x to the y
* `[903bb70]` Q2b: remove old arithmetic oprations

## Step 2c

**1. Approach and Decision:**
From the error messages generated by the `a2_q2_v2` test on the webside server. I found that the customer retained the previous exponentiation operations 
but introduced two new string operations. The new function now needs to return `repeat_x_times` (the character "x" repeated x times) and `repeat_y_times`
(the string representation of y repeated y times). Finally, to ensure code quality, I dedicated a third commit to adding type hints to the function 
signature.

**2. Git Commands Used:**
* `nano a2_q2.py`
* `git add a2_q2.py`
* `git commit -m "Q2c: ..." -m "..."`
* `git push origin main`

**3. Relevant Commit IDs:**
* `[fe2c138]` Q2c: (refactor) add type hints to function signature
* `[eff0492]` Q2c: (feat) add repeat_t_times to the dictionary
* `[ae8c9ca]` Q2c: (feat) add repeat_x_times to the dictionary

## Step 2d

**1. Approach and Decision**
From the error messages generated by `a2_q2_v3` test on the webside server. The customer indicated one last requirement: the addition of the 
`xth_alphabet_repeat_y_times` key. This required identifying the x-th letter of the lowercase English alphabet and repeat it y times. In order to 
accomplish this task, I imported Python's built-in `string` module and utilized `string.ascii_lowercase[x-1]` to accurately retrieve the target character. 
I then combined this character with string multiplication to meet the final requirement. 

**2. Git Commands Used:**
* `nano a2_q2.py`
* `git add a2_q2.py`
* `git commit -m "Q2d: ..." -m "..."`
* `git push origin main`

**3. Relevant Commit IDs:**
* `[9ffeeeb]` Q2d: (docs) explain zero-based indexing for alphabet string
* `[d036e06]` Q2d: (feat) add xth_alphabet_repeat_y_times to answer
* `[b0466eb]` Q2d: (feat) import string module to find the x-th letter
